/**
 * Make sure there is some js code before the environment var.
 * 
 * @type String
 */
var license = 'MIT';

/**
 * This environment will be updated during the test.
 * 
 * @type String
 */
var environment = 'development';

/**
 * Make sure there is some js code after the environment var.
 * 
 * @return {String|environment}
 */
function getEnv()
{
    // End.
    return env;
}