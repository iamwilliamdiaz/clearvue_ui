/**
 * Make sure there is some js code before the version var.
 * 
 * @type String
 */
var license = 'MIT';

/**
 * This version number will be updated during the test.
 * 
 * @type String
 */
var version = '1';

/**
 * Make sure there is some js code after the version var.
 * 
 * @return {String|version}
 */
function getVersion()
{
    // End.
    return version;
}